"use strict"; // treat all JS code as newer version

// alert(3 + 3)  we are using nodeJS, not browser

let name = "krishna";
let isAccount = true;
let a = 3;
let n = null;

/*premitive dataTypes:
number => 2 to power 53
bigint => for bigger values
string => ""
boolean => true/false
null => standalone and object
undefined => 
symbol => unique */

// object

console.log(typeof n);